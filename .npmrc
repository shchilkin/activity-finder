# Ensure optional dependencies are installed (for native binaries like lightningcss)
optional=true

# Use exact versions from package-lock.json
package-lock=true

# Automatically rebuild native modules
rebuild-bundle=true
